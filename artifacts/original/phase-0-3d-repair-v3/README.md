# Phase 0.3 Quantum Aperture Station

This package is the bounded, still-only Phase 0.3 creative redirect. It preserves the Phase 0.1 and Phase 0.2 packages and introduces an original, ground-anchored Quantum Aperture Station. It contains no reference binaries, stock models, external image textures, linked libraries, bundled fonts, video, frame sequence, or production route.

## Selected direction

- Option A: Inclined Optical Blade.
- Nominal installed dimensions: 1.86 W × 1.24 H × 0.72 D m.
- Faceted asymmetric blade, subordinate load-bearing service spine, and below-grade foundation.
- Deep offset horizontal aperture with a lower-right structural interruption; no applied ring, lens, literal Q logo, front plug, feet, plinth, vents, buttons, or appliance seams.
- Desktop uses one continuous 2.5-turn physical cable. Portrait uses an independently authored, tighter 2.25-turn cable. The render controller exposes exactly one family at a time; both retain graphite shoulders and a narrow recessed conductor.
- Exactly one activation mechanism: one linked five-blade internal iris. The blades stay matte and subordinate.
- Five still-camera checkpoints at −14°, −7°, 0°, +7°, and +14°: a 28° total arc.

## Causal activation states

1. Dormant: zero conductor/UI emission; smoked aperture materially black.
2. Current reaches the foundation: only a small flush seam response; aperture remains black.
3. Internal transfer: a restrained internal datum becomes visible; iris stays dormant.
4. Internal iris begins: the linked five-blade mechanism starts once and the first interface meaning appears.
5. Portal ready: sparse semantic operating information is readable.

## Maintainable source

- `source/quantum-aperture-station-v3.blend` is the editable source.
- `source/build_final_station.py` deterministically rebuilds it.
- `source/render_final_stills.py` renders only bounded still groups.
- `source/compose_portal_surfaces.py` consumes `portal-layout.json`.
- `source/compose_final_review.py` composes the non-browser review originals.
- `source/compose_browser_review.py` composes the four browser-derived review originals from hash-bound normalized captures without resampling their pixels.
- `source/validate_final_scene.py` performs Blender-native source validation.
- `source/emit_scene_keepouts.py` projects evaluated station/cable geometry into authoritative source-pixel keepouts for browser collision QA.
- `source/sanitize_png_metadata.py` strips nonessential PNG metadata while proving decoded pixels are unchanged.

The scene was authored and rendered with the authorized portable Blender 5.2.0 LTS release build `fbe6228777e7`. Canonical stills use EEVEE at 64 temporal AA samples and 64 volumetric samples. EEVEE was retained because the beveled/faceted principal surfaces showed no review-resolution faceting and a Cycles pass was not materially necessary for this bounded still gate. No Cycles comparison is claimed. FFmpeg is not used because this redirect intentionally creates no video or animatic.

All commands are intended to run from the repository root with the locally authorized Blender executable. No tool binaries are stored in Git.

## Review outputs

Nine non-browser originals are produced locally: the silhouette gate, recommended design, material, cable, proving-ground, camera-path, activation, and two reduced-motion sheets. Four exact browser-derived sheets—portal typography, desktop hero, mobile hero, and text zoom/fallback—consume the final 46-case/36-capture repository-native Chromium matrix at `artifacts/evidence/phase-0-3d-repair-v3/browser-matrix-report.json`, SHA-256 `8272764a01ac18b4aed7b8b0ebffdca812a5f235e51d6c2c2ea6ae744c6ac4fc`. The compositor pastes normalized browser rasters 1:1, adds labels outside their bounds, and does not repaint or resample captured pixels. The sealed review bundle contains exactly thirteen PNGs.

The matrix uses forced metric-conscious system fallbacks and records 46/46 passing cases, 36/36 normalized visual captures, zero word-fragmentation offenders, and distinct neutral/focus states. Neutral review sources have no keyboard-focus ring; the dedicated focus cases preserve the visible ring for accessibility evidence. Intended Syne, Newsreader, and Inter integration remains a future licensed-font rerun requirement.

The shared portal geometry authority is `portal-layout.json`, SHA-256 `25666cf071afe7564dc051cbec770ead325cdf19ef1f4926e43d793a2a053bc5`.
