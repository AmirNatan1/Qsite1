# Phase 7A reference mechanics and originality record

Status: original Quantum-Hub prototype; all creative gates remain **PENDING HUMAN REVIEW**.

## Reference policy

No third-party page layout, source code, shader, asset, brand composition or
distinctive art direction was copied. Award/catalogue sites were not used as a
source of implementation code. The mechanics adopted in this prototype come
from first-party accepted work and open platform behavior.

| Reference source | Behavior studied | Quantum-Hub fit | Deliberate change | Implementation | Reduced-motion alternative | Performance result |
| --- | --- | --- | --- | --- | --- | --- |
| Accepted Phase 6 physical opening at `371e3e8a…` | Causal current enters the CRT; Q, line, raster and threshold retain one physical narrative | It is the binding source of the Signal Field concept | Physical F1–F500 is not imitated or rerendered; the live field begins only after F500 and derives from the same Q/signal logic | Existing paused H.264 scroll controller plus original DOM/SVG/CSS field | Dormant authoritative poster, resolved static field and full text | Physical mappings/assets remain under exact hash and regression authority; fresh lifecycle figures are reported separately |
| HTML Living Standard disclosure model (`details`/`summary`) | A native disclosure keeps content and links usable without script | Field Map must be understandable, keyboard-accessible and no-JS safe | Replaced menu-like chrome with an authored full-field plane, coordinates and route trajectories; ordinary link behavior remains untouched | Native `details`, `summary`, `nav`, ordered list and links; small Escape/focus enhancement | Direct full-screen semantic list, no scale transition | No dependency; one bounded focus RAF only when opening |
| CSS Media Queries interaction features (`hover`, `pointer`) | Distinguish fine hover probes from coarse/touch input | Pointer response is optional and must never become a touch requirement | No custom cursor, follower, lag, particles or trail; only local material pressure inside the Signal Field | Fine-pointer media query plus one dirty RAF per movement batch | Probe is absent; authored static material remains | Zero stationary work; listener/RAF rest state measured in browser QA |
| `prefers-reduced-motion` platform preference | Communicate motion preference without removing content or hierarchy | The physical poster, field, manifesto and navigation must remain premium and complete | Does not globally substitute an empty or duration-zero experience; it selects an authored resolved composition | Prepaint cinematic bypass plus route CSS alternative | This is the alternative itself | Zero unnecessary cinematic requests is a required measured invariant |
| OpenType variable width/weight axes in legally licensed candidate fonts | Width can express stored/compressed → resolved text without rasterizing letters | Gives the manifesto an architectural causal transition while preserving live DOM words | Axis extremes, random per-letter motion and proprietary benchmark fonts are rejected | Anybody provisional variable WOFF2, conservative width/weight band, whole-word spans | Final readable proportions shown immediately | 69,612-byte production font; full delta measured after final build |

## Originality confirmation

The structural paths, Q/source relationship, magenta contact point, manifesto
composition, field-map coordinates, motion timing and responsive states were
authored for Quantum-Hub. The SVG paths are original project source. No
third-party artwork or proprietary shader is present.

## Performance status

Static design inspection confirms no new runtime dependency, canvas, shader,
interval or perpetual animation loop. Exact browser-measured performance values
remain **NOT OBSERVED** until the final Phase 7A browser evidence run; they must
not be promoted to PASS from source inspection alone.

Phase 7B is **NOT AUTHORIZED**. Main is **NOT MERGED**.

